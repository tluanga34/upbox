import style from "./catalog.module.scss"

export default function PortraitCatalog({className=''}) {
  return (
    <div className={`${className} ${style.portraitCatalogContainer} d-flex justify-content-space-between`}>
      
    </div>
  )
}